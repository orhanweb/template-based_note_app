import 'package:flutter/material.dart';

class CShemes {
  static final ColorScheme appLightColors = ColorScheme.fromSeed(
      seedColor: const Color.fromARGB(255, 11, 69, 156),
      brightness: Brightness.light);

  // Material blue
  static final ColorScheme appDarkColors = ColorScheme.fromSeed(
      seedColor: const Color.fromARGB(255, 33, 212, 243),
      brightness: Brightness.dark);
}
