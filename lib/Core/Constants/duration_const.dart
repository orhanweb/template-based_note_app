class AppDuration {
  static const Duration durationLow = Duration(milliseconds: 500);
  static const Duration durationNormal = Duration(seconds: 1);
  static const Duration durationSlow = Duration(seconds: 2);
}
